diff --git a/cmake/dtrace-utils.cmake b/cmake/dtrace-utils.cmake
index d1402d7..e0010c1 100644
--- a/cmake/dtrace-utils.cmake
+++ b/cmake/dtrace-utils.cmake
@@ -5,9 +5,9 @@ FUNCTION (CHECK_DTRACE d_file)
     IF ((CMAKE_SYSTEM_NAME STREQUAL "Darwin") OR (CMAKE_SYSTEM_NAME STREQUAL "Linux"))
         # USDT is not (yet) supported on platforms (e.g., FreeBSD, Solaris) that require pre-link modification of .o files
         EXECUTE_PROCESS(
-            COMMAND dtrace -o .tmp.dprobes.h -s ${d_file} -h
+            COMMAND dtrace -o ${CMAKE_CURRENT_BINARY_DIR}/.tmp.dprobes.h -s ${d_file} -h
             RESULT_VARIABLE DTRACE_RESULT)
-        FILE(REMOVE .tmp.dprobes.h)
+        FILE(REMOVE ${CMAKE_CURRENT_BINARY_DIR}/.tmp.dprobes.h)
         IF (DTRACE_RESULT EQUAL 0)
             MESSAGE(STATUS "Detecting USDT support - found")
             SET(HAVE_DTRACE "ON" PARENT_SCOPE)
diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -12,2 +12,2 @@ PROJECT(picotls)
-FIND_PACKAGE(PkgConfig REQUIRED)
+FIND_PACKAGE(PkgConfig QUIET)
 INCLUDE(cmake/boringssl-adjust.cmake)
@@ -38,3 +38,6 @@ INCLUDE_DIRECTORIES(
     include
     ${CMAKE_CURRENT_BINARY_DIR})
+IF (WIN32)
+    INCLUDE_DIRECTORIES(picotlsvs/picotls)
+ENDIF ()
 SET(MINICRYPTO_LIBRARY_FILES
@@ -56,4 +59,7 @@ SET(CORE_FILES
     lib/hpke.c
     lib/picotls.c
     lib/pembase64.c)
+IF (WIN32)
+    LIST(APPEND CORE_FILES picotlsvs/picotls/wintimeofday.c)
+ENDIF ()
 SET(CORE_TEST_FILES
@@ -74,3 +80,5 @@ ENDIF ()
-PKG_CHECK_MODULES(BROTLI_DEC libbrotlidec)
-PKG_CHECK_MODULES(BROTLI_ENC libbrotlienc)
+IF (PkgConfig_FOUND)
+    PKG_CHECK_MODULES(BROTLI_DEC libbrotlidec)
+    PKG_CHECK_MODULES(BROTLI_ENC libbrotlienc)
+ENDIF ()
 IF (BROTLI_DEC_FOUND AND BROTLI_ENC_FOUND)
diff --git a/include/picotls.h b/include/picotls.h
--- a/include/picotls.h
+++ b/include/picotls.h
@@ -1420,6 +1420,9 @@ typedef struct st_ptls_log_getsni_t {
         body                                                                                                                       \
     }                                                                                                                              \
     static inline ptls_log_getsni_t ptls_log_getsni_##suffix(type arg)                                                             \
     {                                                                                                                              \
-        return (ptls_log_getsni_t){ptls_log_getsni_cb_##suffix, arg};                                                              \
+        ptls_log_getsni_t result;                                                                                                  \
+        result.cb = ptls_log_getsni_cb_##suffix;                                                                                   \
+        result.arg = arg;                                                                                                          \
+        return result;                                                                                                             \
     }
@@ -2041,4 +2044,8 @@ inline void ptls_buffer_dispose(ptls_buffer_t *buf)
 {
     ptls_buffer__release_memory(buf);
-    *buf = (ptls_buffer_t){NULL, 0, 0, 0, 0};
+    buf->base = NULL;
+    buf->capacity = 0;
+    buf->off = 0;
+    buf->is_allocated = 0;
+    buf->align_bits = 0;
 }

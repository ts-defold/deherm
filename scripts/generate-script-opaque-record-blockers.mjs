#!/usr/bin/env node
import { createHash } from "node:crypto";
import { readFile, writeFile } from "node:fs/promises";
import { pathToFileURL } from "node:url";
import { stableBindingId } from "./lib/binding-identity.mjs";
const root = new URL("../", import.meta.url);
const paths = { ir: new URL("packages/bindings/generated/defold-script-api-ir.json", root), frontier: new URL("packages/bindings/generated/defold-script-table-record-bindings.json", root), policy: new URL("packages/bindings/overrides/script-opaque-record-blockers.json", root), report: new URL("packages/bindings/generated/defold-script-opaque-record-blockers.json", root), target: new URL("packages/sdk/src/generated/script/opaque-record-blockers.ts", root) };
const assert = (value, message) => { if (!value) throw new Error(message); }; const sha256 = (value) => createHash("sha256").update(value).digest("hex"); const compare = (a, b) => a < b ? -1 : a > b ? 1 : 0;
export async function loadInputs() { const [irText, frontierText, policyText] = await Promise.all([paths.ir, paths.frontier, paths.policy].map((path) => readFile(path, "utf8"))); const policy = JSON.parse(policyText); const sourceTexts = new Map(await Promise.all(policy.sources.map(async ({ path }) => [path, await readFile(new URL(`upstream/defold/${path}`, root), "utf8")]))); return { irText, frontierText, policyText, sourceTexts }; }
export function generate(inputs) {
  const ir = JSON.parse(inputs.irText), frontier = JSON.parse(inputs.frontierText), policy = JSON.parse(inputs.policyText);
  assert(policy.schemaVersion === 1 && Array.isArray(policy.sources) && Array.isArray(policy.routes), "opaque-record blocker policy schema is unsupported"); assert(ir.schemaVersion === 1 && frontier.schemaVersion === 1 && ir.defoldRevision === frontier.defoldRevision, "opaque-record inputs drifted");
  const sources = new Map(); for (const source of policy.sources) { const text = inputs.sourceTexts.get(source.path); assert(!sources.has(source.key) && typeof text === "string" && sha256(text) === source.sha256, `${source.path}: pinned opaque-record source drifted`); sources.set(source.key, { ...source, text }); }
  const frontierIds = new Set(frontier.blockedRoutes.filter(({ blocker }) => blocker === "opaque-or-nested-record").map(({ id }) => id)); assert(frontierIds.size === policy.expectedRouteCount, `opaque-record frontier census drifted: ${frontierIds.size}`); const fnById = new Map(ir.functions.map((fn) => [fn.id, fn])); const seen = new Set();
  const routes = policy.routes.map((rule) => { assert(!seen.has(rule.id), `${rule.id}: duplicate opaque-record blocker`); seen.add(rule.id); assert(frontierIds.has(rule.id), `${rule.id}: route left opaque-record frontier`); const source = sources.get(rule.source), fn = fnById.get(rule.id); assert(source && fn && rule.anchors.every((anchor) => source.text.includes(anchor)), `${rule.id}: opaque-record source anchor drifted`); return { id: rule.id, stableId: stableBindingId(rule.id), parameters: fn.parameters.map(({ rawName, rawType, optional }) => ({ name: rawName, rawType, optional })), blocker: rule.blocker, source: `upstream/defold/${source.path}`, sourceSha256: source.sha256, anchors: rule.anchors }; }).sort((a, b) => a.stableId - b.stableId || compare(a.id, b.id));
  assert(seen.size === frontierIds.size && [...frontierIds].every((id) => seen.has(id)), "opaque-record blocker policy does not cover the complete frontier");
  const report = { schemaVersion: 1, defoldRevision: ir.defoldRevision, scope: "All opaque/nested records blocked by the table-record frontier", routeCount: routes.length, candidateCount: 0, executableCount: 0, coverageClaim: "No bounded generator is safe. render.draw borrows predicate and constant-buffer userdata while allocating/transferring frustum command data into the render command queue; render.dispatch_compute borrows a constant buffer and requires an active render-script command context. Neither ownership/lifetime contract exists in ScriptCallFrame.", inputEvidence: { scriptIrSha256: sha256(inputs.irText), tableRecordFrontierSha256: sha256(inputs.frontierText), reviewedPolicySha256: sha256(inputs.policyText), defoldSources: policy.sources.map(({ path, sha256: hash }) => ({ path: `upstream/defold/${path}`, sha256: hash })) }, routes };
  const target = `// Generated by scripts/generate-script-opaque-record-blockers.mjs. Do not edit.\nexport const scriptOpaqueRecordBlockers = ${JSON.stringify(routes.map(({ id, stableId, blocker }) => ({ id, stableId: `0x${stableId.toString(16).padStart(8, "0")}`, blocker })), null, 2)} as const;\nexport function assertScriptOpaqueRecordTargetSupport(_: number): never { throw new Error("Opaque render records have no generated provider: borrowed userdata and command-queue ownership are not represented"); }\n`;
  return { report, target };
}
export async function run(check = false) { const generated = generate(await loadInputs()); const outputs = [[paths.report, `${JSON.stringify(generated.report, null, 2)}\n`], [paths.target, generated.target]]; for (const [path, expected] of outputs) { if (check) assert(await readFile(path, "utf8") === expected, `${path.pathname}: opaque-record blocker output is stale`); else await writeFile(path, expected); } return generated.report; }
if (process.argv[1] && pathToFileURL(process.argv[1]).href === import.meta.url) run(process.argv.includes("--check")).then((report) => console.log(`${process.argv.includes("--check") ? "Verified" : "Generated"} ${report.routeCount} opaque-record blockers; ${report.candidateCount} candidates.`)).catch((error) => { console.error(error.stack ?? error.message); process.exitCode = 1; });

// Backward-compatible CLI import. Identifier policy is compiler-owned because
// SDK emitters and the project generator must make the same naming decisions.
export * from "../../compiler/src/names.mjs";

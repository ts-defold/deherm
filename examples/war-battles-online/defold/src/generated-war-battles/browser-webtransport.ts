// Generated by integration/sync-defold-sources.mjs from core/browser-webtransport.ts. Do not edit.
import {
  TRANSPORT_CHANNEL_SNAPSHOT,
  validateReliableChannel,
  type GameTransport,
  type ReliableChannel,
  type SendDisposition,
  type TransportCapabilities,
  type TransportReceiver,
} from "./transport";
import {
  MAX_RELIABLE_MESSAGE_BYTES,
  RELIABLE_FRAME_HEADER_BYTES,
  ReliableFrameDecoder,
  encodeReliableFrame,
} from "./webtransport-framing";
const DATAGRAM_STAGING_SLOTS = 4;
// Reliable control/session frames are events: unlike snapshots, they cannot
// be replaced. Keep their userland queue deliberately small and fail closed
// rather than allowing a stalled peer to grow memory without bound.
const MAX_PENDING_SERVER_RELIABLE_FRAMES = 32;
const MAX_PENDING_SERVER_RELIABLE_BYTES = 256 * 1024;
type ReliableStreamMode = "client" | "server";

interface PendingServerReliableFrame {
  readonly frame: Uint8Array;
  readonly resolve: (disposition: SendDisposition) => void;
}

export interface WebTransportDatagramsLike {
  readonly readable: ReadableStreamLike<Uint8Array>;
  readonly writable: WritableStreamLike<Uint8Array>;
  readonly maxDatagramSize?: number;
}

export type ReadResultLike<T> =
  | { readonly done: true; readonly value?: undefined }
  | { readonly done: false; readonly value: T };

export interface ReadableStreamReaderLike<T> {
  read(): Promise<ReadResultLike<T>>;
  cancel(reason?: unknown): Promise<void>;
  releaseLock(): void;
}

export interface ReadableStreamLike<T> {
  cancel(reason?: unknown): Promise<void>;
  getReader(): ReadableStreamReaderLike<T>;
}

export interface WritableStreamWriterLike<T> {
  readonly ready: Promise<void>;
  readonly desiredSize: number | null;
  write(chunk: T): Promise<void>;
  close(): Promise<void>;
  abort(reason?: unknown): Promise<void>;
  releaseLock(): void;
}

export interface WritableStreamLike<T> {
  abort(reason?: unknown): Promise<void>;
  close(): Promise<void>;
  getWriter(): WritableStreamWriterLike<T>;
}

export interface WebTransportBidirectionalStreamLike {
  readonly readable: ReadableStreamLike<Uint8Array>;
  readonly writable: WritableStreamLike<Uint8Array>;
}

export interface WebTransportSessionLike {
  readonly ready: Promise<void>;
  readonly closed: Promise<{ closeCode?: number; reason?: string }>;
  readonly datagrams: WebTransportDatagramsLike;
  readonly incomingUnidirectionalStreams: ReadableStreamLike<ReadableStreamLike<Uint8Array>>;
  readonly incomingBidirectionalStreams: ReadableStreamLike<WebTransportBidirectionalStreamLike>;
  createUnidirectionalStream(): Promise<WritableStreamLike<Uint8Array>>;
  createBidirectionalStream(): Promise<WebTransportBidirectionalStreamLike>;
  close(options?: { closeCode?: number; reason?: string }): void;
}

export interface WebTransportConnectionOptions {
  readonly serverCertificateHashes?: readonly { algorithm: string; value: ArrayBuffer }[];
  /** Session-local credits for server snapshots/control messages. */
  readonly anticipatedConcurrentIncomingUnidirectionalStreams?: number;
  /** Reserved for servers that choose a bidirectional reliable lane. */
  readonly anticipatedConcurrentIncomingBidirectionalStreams?: number;
}

export interface WebTransportConstructorLike {
  new (url: string, options?: WebTransportConnectionOptions): WebTransportSessionLike;
}

/**
 * WebTransport-shaped game adapter shared by browser and native Defold. The
 * selected constructor owns the platform implementation. Client-originated reliable messages use
 * independent QUIC bidirectional streams. Server-originated reliable messages
 * share one persistent unidirectional stream, with the existing five-byte
 * frames repeated on that stream. Tick inputs use datagrams only when the
 * negotiated session exposes them and the writable queue can accept data
 * immediately.
 */
export class WebTransportGameClient implements GameTransport {
  readonly capabilities: TransportCapabilities;
  private readonly datagramWriter: WritableStreamWriterLike<Uint8Array>;
  private readonly datagramStaging: Uint8Array[];
  private readonly datagramStagingBusy: boolean[];
  private readonly session: WebTransportSessionLike;
  private readonly receiver: TransportReceiver;
  private readonly reliableStreamMode: ReliableStreamMode;
  private serverReliableWriter?: WritableStreamWriterLike<Uint8Array>;
  private serverReliableWriterPromise?: Promise<WritableStreamWriterLike<Uint8Array> | undefined>;
  private serverReliableWrite?: Promise<void>;
  private readonly pendingServerReliable: PendingServerReliableFrame[] = [];
  private pendingServerReliableBytes = 0;
  private pendingServerSnapshot?: Uint8Array;
  private snapshotFlushScheduled = false;
  private snapshotFlushActive = false;
  private datagramWriterClosed = false;
  private sessionCloseRequested = false;
  private closed = false;

  private constructor(
    session: WebTransportSessionLike,
    receiver: TransportReceiver,
    reliableStreamMode: ReliableStreamMode,
  ) {
    this.session = session;
    this.receiver = receiver;
    this.reliableStreamMode = reliableStreamMode;
    const maxDatagramBytes = session.datagrams.maxDatagramSize ?? 0;
    this.capabilities = Object.freeze({
      protocol: "webtransport-h3" as const,
      reliableStreams: true,
      datagrams: maxDatagramBytes > 0,
      maxDatagramBytes,
    });
    this.datagramWriter = session.datagrams.writable.getWriter();
    // A datagram write may remain pending after the caller reuses its input
    // buffer. Own at most four in-flight datagrams in preallocated storage;
    // no gameplay-path queue can grow beyond this fixed bound.
    this.datagramStaging = [];
    this.datagramStagingBusy = [];
    if (maxDatagramBytes > 0) {
      for (let index = 0; index < DATAGRAM_STAGING_SLOTS; index += 1) {
        this.datagramStaging.push(new Uint8Array(maxDatagramBytes));
        this.datagramStagingBusy.push(false);
      }
    }
  }

  static async connect(
    url: string,
    receiver: TransportReceiver,
    constructorOverride?: WebTransportConstructorLike,
    options?: WebTransportConnectionOptions,
  ): Promise<WebTransportGameClient> {
    const Constructor = constructorOverride ?? browserWebTransportConstructor();
    const session = new Constructor(url, {
      // WebTransport session stream credits are separate from the QUIC
      // connection's limits. Chromium otherwise grants only one server stream,
      // which is enough for welcome but starves the 20 Hz snapshot lane.
      anticipatedConcurrentIncomingUnidirectionalStreams: 64,
      anticipatedConcurrentIncomingBidirectionalStreams: 8,
      ...options,
    });
    await session.ready;
    return WebTransportGameClient.adopt(session, receiver, "client");
  }

  private static adopt(
    session: WebTransportSessionLike,
    receiver: TransportReceiver,
    reliableStreamMode: ReliableStreamMode,
  ): WebTransportGameClient {
    const client = new WebTransportGameClient(session, receiver, reliableStreamMode);
    void client.receiveReliableStreams();
    void client.receiveDatagrams();
    void session.closed.then(
      (close) => client.finishClose(close.closeCode ?? 0, close.reason ?? "transport closed", false),
      (error: unknown) => client.finishClose(1, error instanceof Error ? error.message : "transport failed"),
    );
    return client;
  }

  static adoptServer(session: WebTransportSessionLike, receiver: TransportReceiver): WebTransportGameClient {
    return WebTransportGameClient.adopt(session, receiver, "server");
  }

  async sendReliable(channel: ReliableChannel, payload: Uint8Array, signal?: AbortSignal): Promise<SendDisposition> {
    if (this.closed || signal?.aborted === true) return "closed";
    validateReliableChannel(channel);
    if (payload.byteLength > MAX_RELIABLE_MESSAGE_BYTES) return "too-large";

    if (this.reliableStreamMode === "server") {
      // sendServerReliable builds the owned frame synchronously before its
      // first queue or writer boundary, so a second payload copy would only
      // add allocation pressure to the 20 Hz snapshot path.
      return this.sendServerReliable(channel, payload);
    }

    // Own the bytes before any stream creation, queueing, or other await. The
    // caller is free to reuse or mutate its buffer as soon as this method
    // returns a promise.
    const ownedPayload = payload.slice();

    let reverseReadable: ReadableStreamLike<Uint8Array> | undefined;
    let writer: WritableStreamWriterLike<Uint8Array> | undefined;
    const abort = (): void => {
      void writer?.abort(signal?.reason);
    };
    signal?.addEventListener("abort", abort, { once: true });
    try {
      let writable: WritableStreamLike<Uint8Array>;
      if (this.reliableStreamMode === "client") {
        const stream = await this.session.createBidirectionalStream();
        reverseReadable = stream.readable;
        writable = stream.writable;
      } else {
        writable = await this.session.createUnidirectionalStream();
      }
      writer = writable.getWriter();
      const header = new Uint8Array(RELIABLE_FRAME_HEADER_BYTES);
      const view = new DataView(header.buffer);
      view.setUint8(0, channel);
      view.setUint32(1, ownedPayload.byteLength, true);
      await writer.ready;
      await writer.write(header);
      await writer.write(ownedPayload);
      await writer.close();
      return "sent";
    } catch {
      return "closed";
    } finally {
      signal?.removeEventListener("abort", abort);
      writer?.releaseLock();
      if (reverseReadable !== undefined) {
        try {
          void reverseReadable.cancel().catch(() => undefined);
        } catch {
          // A stream may already have been reset by the peer.
        }
      }
    }
  }

  async trySendDatagram(payload: Uint8Array): Promise<SendDisposition> {
    if (this.closed) return "closed";
    if (!this.capabilities.datagrams || payload.byteLength > this.capabilities.maxDatagramBytes) return "too-large";
    // Do not await writer.ready here: waiting would turn stale inputs into a queue.
    if ((this.datagramWriter.desiredSize ?? 0) <= 0) return "backpressured";
    let slotIndex = -1;
    for (let index = 0; index < this.datagramStagingBusy.length; index += 1) {
      if (!this.datagramStagingBusy[index]) {
        slotIndex = index;
        break;
      }
    }
    if (slotIndex < 0) return "backpressured";
    const slot = this.datagramStaging[slotIndex]!;
    slot.set(payload);
    this.datagramStagingBusy[slotIndex] = true;
    try {
      // The subarray is only a bounded view over the fixed slot; the bytes it
      // exposes remain owned by this adapter until the write settles.
      await this.datagramWriter.write(slot.subarray(0, payload.byteLength));
      return "sent";
    } catch {
      return "closed";
    } finally {
      this.datagramStagingBusy[slotIndex] = false;
    }
  }

  close(code: number, reason: string): void {
    if (this.closed) return;
    this.finishClose(code, reason);
  }

  private async receiveReliableStreams(): Promise<void> {
    const reader =
      this.reliableStreamMode === "client"
        ? this.session.incomingUnidirectionalStreams.getReader()
        : this.session.incomingBidirectionalStreams.getReader();
    try {
      while (!this.closed) {
        const next = await reader.read();
        if (next.done) break;
        const stream =
          this.reliableStreamMode === "client"
            ? (next.value as ReadableStreamLike<Uint8Array>)
            : (next.value as WebTransportBidirectionalStreamLike).readable;
        const reverseWritable =
          this.reliableStreamMode === "server"
            ? (next.value as WebTransportBidirectionalStreamLike).writable
            : undefined;
        void this.readReliableStream(stream, reverseWritable);
      }
    } catch (error: unknown) {
      this.finishClose(1, error instanceof Error ? error.message : "reliable stream receive failed");
    } finally {
      reader.releaseLock();
    }
  }

  private async readReliableStream(
    stream: ReadableStreamLike<Uint8Array>,
    reverseWritable?: WritableStreamLike<Uint8Array>,
  ): Promise<void> {
    const reader = stream.getReader();
    let reverseWriter: WritableStreamWriterLike<Uint8Array> | undefined;
    if (reverseWritable !== undefined) {
      try {
        reverseWriter = reverseWritable.getWriter();
      } catch {
        // A peer may reset the reverse direction before the stream is read.
      }
    }
    const decoder = new ReliableFrameDecoder();
    let receivedBytes = false;
    try {
      while (true) {
        const next = await reader.read();
        if (next.done) break;
        if (next.value.byteLength > 0) receivedBytes = true;
        decoder.push(next.value, (channel, payload) => this.receiver.onReliable(channel, payload));
      }
      if (!receivedBytes) throw new Error("truncated reliable message");
      decoder.finish();
    } catch (error: unknown) {
      this.finishClose(2, error instanceof Error ? error.message : "invalid reliable message");
    } finally {
      reader.releaseLock();
      if (reverseWriter !== undefined) {
        try {
          await reverseWriter.close();
        } catch {
          // Closing a peer-reset reverse direction is a benign stream-local
          // failure and must not tear down an otherwise healthy session.
        }
        try {
          reverseWriter.releaseLock();
        } catch {
          // The stream may have released the lock as part of peer reset.
        }
      }
    }
  }

  private async receiveDatagrams(): Promise<void> {
    const reader = this.session.datagrams.readable.getReader();
    try {
      while (!this.closed) {
        const next = await reader.read();
        if (next.done) break;
        this.receiver.onDatagram(next.value);
      }
    } catch (error: unknown) {
      this.finishClose(1, error instanceof Error ? error.message : "datagram receive failed");
    } finally {
      reader.releaseLock();
    }
  }

  private finishClose(code: number, reason: string, requestSessionClose = true): void {
    if (this.closed) return;
    this.closed = true;
    this.pendingServerSnapshot = undefined;
    for (const pending of this.pendingServerReliable) pending.resolve("closed");
    this.pendingServerReliable.length = 0;
    this.pendingServerReliableBytes = 0;
    const writer = this.serverReliableWriter;
    this.serverReliableWriter = undefined;
    if (writer !== undefined) {
      void writer.abort(reason).catch(() => undefined);
      writer.releaseLock();
    }
    if (!this.datagramWriterClosed) {
      this.datagramWriterClosed = true;
      try {
        void this.datagramWriter.abort(reason).catch(() => undefined);
      } catch {
        // The session may have already reset the datagram sink.
      }
      try {
        this.datagramWriter.releaseLock();
      } catch {
        // The sink may have released its lock during a peer reset.
      }
    }
    if (requestSessionClose && !this.sessionCloseRequested) {
      this.sessionCloseRequested = true;
      try {
        this.session.close({ closeCode: code, reason });
      } catch {
        // A session can already be terminal while its `closed` promise is
        // still settling. The adapter's close notification remains idempotent.
      }
    }
    this.receiver.onClose(code, reason);
  }

  private async sendServerReliable(channel: ReliableChannel, payload: Uint8Array): Promise<SendDisposition> {
    const frame = encodeReliableFrame(channel, payload);

    if (this.closed) return "closed";

    if (channel === TRANSPORT_CHANNEL_SNAPSHOT) {
      // A snapshot is state, not an event. Keep at most the latest frame while
      // the persistent reliable lane is flow-controlled.
      if (
        this.serverReliableWrite !== undefined ||
        this.pendingServerReliable.length !== 0 ||
        this.serverReliableWriter === undefined ||
        (this.serverReliableWriter.desiredSize ?? 0) <= 0
      ) {
        this.pendingServerSnapshot = frame;
        this.scheduleSnapshotFlush();
        return "backpressured";
      }
      this.pendingServerSnapshot = undefined;
      return this.writeServerFrame(this.serverReliableWriter!, frame);
    }

    // Session/control messages are never silently dropped. Once the writer
    // is busy or flow-controlled, put the owned frame in one bounded FIFO and
    // let the single pump own the sole writer.ready waiter.
    if (
      this.serverReliableWriter === undefined ||
      this.serverReliableWrite !== undefined ||
      this.pendingServerReliable.length !== 0 ||
      (this.serverReliableWriter.desiredSize ?? 0) <= 0
    ) {
      if (
        this.pendingServerReliable.length >= MAX_PENDING_SERVER_RELIABLE_FRAMES ||
        this.pendingServerReliableBytes + frame.byteLength > MAX_PENDING_SERVER_RELIABLE_BYTES
      ) {
        this.finishClose(2, "reliable control queue capacity exceeded");
        return "closed";
      }
      return new Promise((resolve) => {
        this.pendingServerReliable.push({ frame, resolve });
        this.pendingServerReliableBytes += frame.byteLength;
        this.scheduleSnapshotFlush();
      });
    }
    return this.writeServerFrame(this.serverReliableWriter!, frame);
  }

  private async serverReliableWriterOrClosed(): Promise<WritableStreamWriterLike<Uint8Array> | undefined> {
    if (this.closed) return undefined;
    if (this.serverReliableWriter !== undefined) return this.serverReliableWriter;
    if (this.serverReliableWriterPromise === undefined) {
      this.serverReliableWriterPromise = this.session
        .createUnidirectionalStream()
        .then((writable) => {
          if (this.closed) {
            const writer = writable.getWriter();
            void writer.abort("transport closed").catch(() => undefined);
            writer.releaseLock();
            return undefined;
          }
          this.serverReliableWriter = writable.getWriter();
          return this.serverReliableWriter;
        })
        .catch((error: unknown) => {
          this.finishClose(1, error instanceof Error ? error.message : "reliable stream create failed");
          return undefined;
        })
        .finally(() => {
          this.serverReliableWriterPromise = undefined;
        });
    }
    return this.serverReliableWriterPromise;
  }

  private async writeServerFrame(
    writer: WritableStreamWriterLike<Uint8Array>,
    frame: Uint8Array,
  ): Promise<SendDisposition> {
    let write: Promise<void>;
    try {
      write = writer.write(frame);
    } catch {
      this.finishClose(1, "reliable stream write failed");
      return "closed";
    }
    this.serverReliableWrite = write;
    try {
      await write;
      return "sent";
    } catch {
      this.finishClose(1, "reliable stream write failed");
      return "closed";
    } finally {
      if (this.serverReliableWrite === write) this.serverReliableWrite = undefined;
      this.scheduleSnapshotFlush();
    }
  }

  private scheduleSnapshotFlush(): void {
    if (
      this.snapshotFlushScheduled ||
      this.snapshotFlushActive ||
      (this.pendingServerSnapshot === undefined && this.pendingServerReliable.length === 0) ||
      this.closed
    )
      return;
    this.snapshotFlushScheduled = true;
    queueMicrotask(() => {
      this.snapshotFlushScheduled = false;
      if (this.snapshotFlushActive || this.closed) return;
      this.snapshotFlushActive = true;
      void this.flushPendingSnapshot().finally(() => {
        this.snapshotFlushActive = false;
        this.scheduleSnapshotFlush();
      });
    });
  }

  private async flushPendingSnapshot(): Promise<void> {
    if (
      this.closed ||
      (this.pendingServerSnapshot === undefined && this.pendingServerReliable.length === 0) ||
      this.serverReliableWrite !== undefined
    )
      return;
    const writer = await this.serverReliableWriterOrClosed();
    if (writer === undefined || this.closed) return;
    if ((writer.desiredSize ?? 0) <= 0) {
      try {
        await writer.ready;
      } catch {
        this.finishClose(1, "reliable stream backpressure failed");
        return;
      }
      if (this.closed) return;
    }
    if (this.serverReliableWrite !== undefined) {
      this.scheduleSnapshotFlush();
      return;
    }

    const pending = this.pendingServerReliable.shift();
    if (pending !== undefined) {
      this.pendingServerReliableBytes -= pending.frame.byteLength;
      const disposition = await this.writeServerFrame(writer, pending.frame);
      pending.resolve(disposition);
      return;
    }

    if (this.pendingServerSnapshot === undefined) return;
    const frame = this.pendingServerSnapshot;
    this.pendingServerSnapshot = undefined;
    if ((await this.writeServerFrame(writer, frame)) === "closed") this.pendingServerSnapshot = undefined;
  }
}

/**
 * Adopts an already-authenticated server-side session exposed through the same
 * stream/datagram primitives. A Rust/moq-dev or other backend owns TLS, HTTP/3,
 * origin checks, and session acceptance before calling this boundary.
 */
export async function adoptServerWebTransportSession(
  session: WebTransportSessionLike,
  receiver: TransportReceiver,
): Promise<GameTransport> {
  await session.ready;
  return WebTransportGameClient.adoptServer(session, receiver);
}

function browserWebTransportConstructor(): WebTransportConstructorLike {
  const candidate = (globalThis as typeof globalThis & { WebTransport?: WebTransportConstructorLike }).WebTransport;
  if (candidate === undefined) throw new Error("WebTransport is unavailable in this browser/runtime");
  return candidate;
}
